import 'package:flutter/material.dart';

class MyListCard extends StatelessWidget {
  const MyListCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
