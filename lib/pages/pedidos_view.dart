import 'package:flutter/material.dart';

class PedidosViewScreen extends StatelessWidget {
  const PedidosViewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
